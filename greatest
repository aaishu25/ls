echo "Enter A"
read A
echo "Enter B"
read B
echo "Enter C" 
read C

if [ $A -gt $B ] && [ $A -gt $C ]; then
    echo "$A is greatest"
elif [ $B -gt $A ] && [ $B -gt $C ]; then
    echo "$B is greatest"
else
    echo "$C is the greatest"
fi
